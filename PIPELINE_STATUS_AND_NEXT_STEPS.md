Memory Machine — Thesis-Final Pipeline: Status & Next Steps
=============================================================
*Last updated: 2026-07-08. Written for sharing with Gemini and for picking this back up later — not a permanent spec, a snapshot.*

## 2026-07-08 update: hybrid headless-Blender + Three.js pipeline shipped; entrance-anchor bug found and fixed

- **Viewport lighting/views/export** (`frontend/src/components/Viewport.jsx`): hemisphere fill light + ACES Filmic tone mapping (was flat/dark before); a full architectural view-preset set (Perspective/Axo-Iso/Plan/Front/Side), the orthographic presets using a true `THREE.OrthographicCamera` with zoom auto-derived from live canvas size, no vanishing-point distortion; a simple "Export PNG" button (`canvas.toDataURL()` + `preserveDrawingBuffer`, same technique the old metabolizer prototype used).
- **Real bug found and fixed building the Front/Side presets:** `THREE.InstancedMesh.setColorAt()`/`instanceColor` reliably renders **solid black** specifically when viewed through a true axis-aligned `OrthographicCamera` — reproduced with old/new lighting, with/without tone mapping, with `MeshBasicMaterial` instead of `MeshStandardMaterial`, and with `side: THREE.DoubleSide`; the only fix was dropping `instanceColor` for a plain `material.color` per distinct color group (`TerraceVoxels` now renders one `instancedMesh` per depth level instead of one big mesh with per-instance color). As a side effect this also fixed an incidental double-darkening bug (`material.color × instanceColor` were both being multiplied when usually equal).
- **New hybrid pipeline: headless Blender as an on-demand "full-fidelity build" tier, not part of the live rebuild loop.** The live loop (`logic/pershing_api.py`'s in-process `TerracingEngine` call, ~0.04s) is unchanged and still drives every slider/paint interaction — spawning a Blender process has real multi-second startup cost, so it would have killed the responsiveness the original cockpit POC was built to prove. Instead, a new explicit "Build in Blender" action:
  - `blender/pershing_headless_build.py` — takes the *exact same JSON* `pershing_api.rebuild()` already returns (no `TerracingEngine` re-run, no bpy paint-canvas dependency) and reconstructs the terrace + structural framing mesh via a straight port of `blender_cockpit.py`'s bmesh-building functions (`build_terrace_mesh`, `build_structural_meshes`), exported as a plain real-feet-Z-up OBJ — deliberately does **not** bake in `blender_cockpit.py`'s axo-mirror (that's specific to its own fixed presentation camera; this OBJ needs to match the live JSON's own coordinate convention so it drops into the same `toThree()` transform every other layer uses) and does **not** re-embed static context (the frontend already renders that once from `site_named.obj`).
  - `logic/pershing_blender.py` — async subprocess wrapper (background thread + job-id/poll, so the Blender run never blocks the FastAPI event loop), reusing `logic/blender_demo.py`'s already-proven `BLENDER_PATH` lookup (env var → common install-path candidates, including this machine's actual Blender 5.0 install → bare `blender` on PATH) rather than re-deriving it.
  - `/api/pershing/blender-build` (POST, starts job) + `/api/pershing/blender-build/{job_id}` (GET, polls) in `app.py`; `BlenderBuild.jsx` (new) displays the result via the same `OBJLoader` pattern already proven in `StaticContext.jsx`; a "Build in Blender" button in `ParamPanel.jsx` + a "Blender Build"/"Live View" toggle in `Viewport.jsx`.
  - `blender_cockpit.py`'s own live-paint-in-Blender mode is now functionally redundant (the browser's `PaintOverlay.jsx` covers the same job with less friction) — left in place as a manual fallback, not extended further.
- **Two real bugs found via live verification, not just theorized:**
  1. **No concurrency limit on Blender builds.** Two subprocesses launched close together contended for CPU/disk; one hit the full 180s timeout even though the *same input* completed in 1.9s run alone. Fixed with a non-blocking `threading.Lock` in `logic/pershing_blender.py` — a second build request while one's running now fails immediately with a clear message instead of silently contending.
  2. **Intermittent multi-minute hangs traced to this machine's Blender addon profile**, not the script: the installed `meshgen` addon fails to import (`ModuleNotFoundError: smolagents`) and BlenderMCP auto-registers/unregisters a socket server on every `--background` launch — neither needed for a pure geometry-build-and-export job, and one of them was an unreliable hang source. Fixed by adding `--factory-startup` to the subprocess call (skips the user addon profile entirely for headless runs); re-verified clean and repeatable (1.3-1.9s per build) afterward.
- **Entrance-anchor / "transit attractor" bug found while investigating why the canyon doesn't reach the metro entrance — root-caused, fixed, and verified** (see the dedicated subsection below for full detail). The user restored the `metroConnection` geometry in Rhino mid-session; re-derived the anchor from live geometry via a freshly-calibrated transform and confirmed the fix's real effect via `/api/pershing/rebuild`, not just a code read.
- **Live-Blender-session safety check, while investigating the above:** confirmed (via live introspection of the interactive `blender_cockpit.py` session, not assumption) that its currently-loaded code is byte-identical to what's on disk for `terracing_engine.py`, `blender_cockpit.py`, `sketch_weight_mapper.py`, `amenity_deficit.py` — *except* the session predates the current Image-Paint-based paint mechanism and still has an earlier **Grease Pencil-based** sketch-drawing implementation loaded in memory (`bake_sketch_gp`, `MM_OT_draw_canyon`/`MM_OT_draw_hardscape`, `GP_CANYON_LAYER`/`GP_HARDSCAPE_LAYER`/`GP_BRUSH_RADIUS_FT=12.0`) that **was never committed to git and isn't on disk in any form** — confirmed via `git log -S` across all history. `inspect.getsource()` proved unreliable for recovering it (silently returned other functions' bodies, caught and rejected rather than trusted). Captured what's safely recoverable instead: full bytecode disassembly + preserved docstrings + the exact resolved Grease Pencil v3 API path (`frame.drawing.strokes[i].points[j].position.x/.y`) to `archive/blender_cockpit_grease_pencil_bytecode_snapshot_20260708.txt`, combined with the existing design spec in `HANDOFF_07052026_GREASE_PENCIL_INTERFACE.md`. User separately saved the live session's `.blend` file to `blender/` — session not yet restarted as of this writing.
- **Rhino `BOUNDARY` curve corrected to real site dimensions.** It had been drawn snapped to the outermost column centers (13.10–341.20 × 7.58–599.33 in site-local ft — confirmed by converting its live coordinates through the same column-calibrated transform used for the anchor fix above), ~26ft/~10.6ft short of the real terrace-derived `site.width_ft`/`site.length_ft` (354.14 × 602.4) on each axis. Replaced with a new closed polyline expanded symmetrically from its original position to the exact real dimensions (verified: new bbox converts to exactly 354.14 × 602.4 site-local). Old curve deleted, new one on the same `BOUNDARY` layer.
- **The `pershingRhinoPlanView.svg` "wildly wrong site dimensions" bug from 2026-07-05 is NOT actually still open — re-verified and it's already fixed.** Ran the real `load_structural_grid_from_svg()` (not a re-derivation) against the actual file with the real 27ft column spacing: returns `site_width_ft=354.99, site_height_ft=603.85`, within 0.24% of the real 354.14×602.4 — nowhere near the previously-reported 64×51ft. The 2026-07-05 fix that searches every `<path>` inside `STRUC__Slabs` (including its `L1`/`L2`/`L3` sublayers) instead of just the first one found — framed at the time as fixing "wrong shape" — was actually the same root cause as the dimension bug (an incomplete slab outline is wrong in both shape and scale). It just never got re-verified afterward, so this doc kept carrying it as open, including in the entry directly above this one earlier today — corrected now after actually re-running the code instead of re-stating the old claim.

### Line Art SVG export wired into the headless-Blender build tier, and a real upstream Blender bug found + worked around (2026-07-08)

- **`blender/pershing_headless_build.py` gained an optional `--lineart-output <svg path>` mode**, added on top of the existing OBJ-export build: `build_line_art()` frames an orthographic axo camera from the just-built scene's own world bounding box (`_scene_bounds_world()`, camera-space-projected `ortho_scale`, same technique already proven in `blender_lineart_export.py`), bakes a `LINEART_SCENE`-preset Grease Pencil object against it, and exports SVG — operating directly on the in-memory mesh, no OBJ round-trip needed.
- **The previously-flagged "minor known bug" (line 144 as it read before today, "one numerically-degenerate coordinate, ~10⁷ magnitude, doesn't affect correctness, just cosmetic") was a significant underestimate — re-investigated today and it's much worse than that characterization.** Verified via a from-scratch Playwright render (`page.goto()` directly on the `file://` SVG, not an `<img>` embed — that approach silently fails to load local SVGs and was a red herring earlier in this investigation) that Blender's own `bpy.ops.wm.grease_pencil_export_svg` operator produces an SVG whose declared `viewBox`/width/height (and, worse, the actual per-point path coordinates themselves) carry a severe, non-uniform X/Y scale corruption — observed aspect ratios from 11:1 up to 99:1 against scenes whose true camera-space aspect (independently computed from `_scene_bounds_world()`'s projected corners, the same math already trusted for `ortho_scale`) is a sane ~1.7:1. A full-canvas render showed real content compressed into a razor-thin sliver, confirming this is genuine geometric distortion, not just a mislabeled canvas.
- **Root-caused to a confirmed, currently-open upstream Blender bug**, not anything in this project's own scene/camera code: [Blender bug #136312](https://projects.blender.org/blender/blender/issues/136312) ("Grease Pencil SVG export — wrong export resolution, should be Output > Resolution X,Y", regression in 4.3+, still open in the 5.0.1 build this machine runs) plus the related [T86942](https://developer.blender.org/T86942) (orthographic-camera Line Art SVG/PDF export issues). Ruled out every scene-side variable first, empirically, before concluding this: forcing a square render resolution changed the distortion's magnitude but didn't eliminate it (11.4:1 → 5.0:1); switching `LINEART_SCENE`→`LINEART_OBJECT` and toggling `use_clip_camera` both reproduced the same class of bug (~99:1) — isolating it to the exporter operator itself, not this script's inputs.
- **A naive post-hoc fix (rescaling just the X coordinates of the exported path data to restore the correct aspect ratio) was tried, verified visually, and rejected** — it "corrected" the aspect ratio number but the rendered result showed long garbage diagonal rays fanning out from the real geometry, proving the corruption isn't a single uniform affine error that a linear correction can undo.
- **Real fix: bypass the buggy exporter operator entirely.** `_export_lineart_svg()` (new, in `pershing_headless_build.py`) reads the baked Grease Pencil stroke points directly (`gp_obj.data.layers[*].frames[*].drawing.strokes[*].points[*].position`, confirmed via introspection to already be plain world-space `Vector`s since the GP object's `matrix_world` is identity), projects each one through the same `cam_obj.matrix_world.inverted()` transform already trusted for `ortho_scale` framing, and writes a minimal hand-built SVG (`<path d="M.. L.. .."/>` per stroke, viewBox fit to the true projected bbox with a small margin) — no dependency on Blender's own page-sizing logic at all. Verified end-to-end via the real script (not a debug harness): both `HEADLESS_BUILD_DONE` and `LINEART_DONE` print cleanly, the output SVG's viewBox aspect (1.67:1) matches the independently-computed camera-space aspect almost exactly, and a full-canvas Playwright render shows a correct, cleanly-proportioned axonometric wireframe of the terrace + structural framing (columns, canyon steps, structural elements all legible) — no clipping, no stray rays, no degenerate points.

### Metro entrance/tunnel "wrong corner" bug — root-caused, fixed, and user-verified in the live app (2026-07-08)

- **User reported** the metro connection/tunnel showing on the opposite corner of the site from where they should be, in the running React app (before any Line Art wiring work started — flagged as a prerequisite to verify first).
- **Root cause: two separate fields for the same real-world object, only one of which got fixed.** `real_geometry.json` carries `secondary_entrance_anchor` (x/z, used only by `terracing_engine.py`'s excavation-influence math) and `secondary_entrance_mesh` (actual displayed triangle geometry). Earlier this session, the anchor was corrected (cross-validated via live Rhino MCP + two independent SVG exports) from a stale position near the site's low end to the true position near length-axis z≈566ft. The **mesh never got the same correction** — it's a separate field, sourced from `extract_real_geometry.py`'s already-known-stale `"object_2"` grid-base-OBJ mapping, and nothing carried the anchor fix over to it. So the live viewport (columns/excavation, driven by the corrected anchor-adjacent data) and the static reference mesh (entrance/tunnel, driven by the untouched stale mesh field) disagreed — exactly the "opposite corner" symptom.
- **Also found stale in the process:** `outputs/vector_export_test/site_named.obj` (the static OBJ `StaticContext.jsx` loads for columns/tunnel/entrance/ramps reference geometry) hadn't been regenerated since 2026-07-03, five days before this session's entrance-anchor and BOUNDARY-curve fixes — a second, compounding staleness bug on top of the mesh-field one.
- **Fix:** `build_real_geometry.py` now translates `secondary_entrance_mesh`'s length-axis vertices so its centroid lands on the corrected `secondary_entrance_anchor.z` (a +539ft shift this run) — X needed no change (`min(mesh x) ≈ anchor.x` already, both sampled from the same real Rhino extrusion). Re-ran the builder, regenerated `site_named.obj` from the corrected `real_geometry.json`, restarted the backend (`app.py`, was stale-loaded in memory from before today's fixes).
- **Deliberately not touched:** `tunnel_mesh`. It's the already-documented placeholder twin-tunnel geometry (see "Next up" #1) — a ~914ft span deliberately extending past both site ends, not a simple translate-to-anchor case like the entrance vestibule. If it's still visibly wrong after this fix, that's the known placeholder being crude, not a new bug.
- **User-verified 2026-07-08: "metro looks good in the app now."** Confirmed visually in the live running app after the backend restart, not just by code inspection — this project's established bar.

### Line Art export wired end-to-end through the app, and browser-verified (2026-07-08)

- **Backend:** `logic/pershing_blender.py`'s `start_build_job()` gained a `lineart: bool` param — when set, passes `--lineart-output <svg path>` to the *same* Blender subprocess alongside `--output <obj path>` (one process, one build, not a second launch — the mesh is already in memory there). Job dict gained an `svg_url` field alongside `obj_url`. `app.py`'s `POST /api/pershing/blender-build` route takes `lineart` as a query param (`?lineart=true`), not a body field, so the POST body stays exactly the raw rebuild-result dict forwarded straight to Blender as input JSON.
- **Frontend:** `api.js`'s `startBlenderBuild()` takes a `lineart` flag; `App.jsx` tracks `lineartEnabled` (checkbox state) and `showLineArt` (overlay visibility), normalizes the job's `svg_url` into `blenderBuild.svgUrl`. `ParamPanel.jsx` got an "Include Line Art SVG" checkbox next to the existing "Build in Blender" button. `Viewport.jsx` got a "View Line Art" button (next to the existing "Blender Build" toggle, only shown once `blenderSvgUrl` exists) that opens a new `LineArtOverlay.jsx` — a plain `<img>` of the SVG (not a parsed/embedded SVG DOM, since the file can be several MB of individual stroke paths) on a white background (the strokes are black-on-transparent), with download and close controls, following the same full-screen-overlay shell `PaintOverlay.jsx` already established for "not the live 3D view, needs real screen space" UI.
- **Real bug hit and fixed during verification, not just assumed working:** first end-to-end browser test showed the "View Line Art" button never appearing, and a direct API check confirmed the job response was missing `svg_url` entirely, with no `.svg` file written despite the frontend correctly sending `?lineart=true`. Root cause: the backend process was still running the *pre-edit* `app.py`/`pershing_blender.py` from an earlier restart (done for the unrelated entrance-anchor fix, before this wiring work started) — Python processes don't pick up source edits without a restart, and this one wasn't using `--reload`. Restarted the backend; a direct `curl -X POST ".../blender-build?lineart=true"` immediately confirmed correct behavior (`svg_url` populated, real `.svg` file on disk, 10.3s duration matching the extra bake+export step).
- **Verified via a real Playwright browser session against the actual running app** (`localhost:5173` dev server + restarted `:8000` backend), not just curl/unit-level checks: checked the "Include Line Art SVG" box, clicked "Build in Blender," confirmed the rebuild log showed `blender build queued: ... (+ line art)` then `blender build done in 17.3s`, clicked the resulting "View Line Art" button, and screenshotted the overlay — shows a correct, cleanly-proportioned axonometric wireframe of the full terraced site (columns, canyon steps, structural framing all legible), matching the quality already confirmed at the Blender-script level earlier this session.

### Entrance-anchor bug — root-caused, fixed, and verified (2026-07-08)

- **What was broken:** `real_geometry.json`'s `secondary_entrance_anchor` (`x=462.99, z=36.29`) drives `terracing_engine.py`'s transit-attractor excavation (`transit_influence = exp(-entrance_dist/transit_falloff_ft)`, `terracing_engine.py:159-160`).
- **Initial diagnosis (confirmed via a real `/api/pershing/rebuild` call, not just theory):** at the app's default (`canyon_width=3`, falloff=81ft, `sketch_alpha=0`) — **zero voxels excavated** from the transit attractor anywhere in the site. This is why the attractor looked "disappeared": at default/narrow settings it contributed literally nothing.
- **The user restored the `metroConnection` object in Rhino mid-session** (it had been missing/unlayered as of the initial investigation). With it back, live-queried its real bounding box and re-derived the site-local transform from scratch using fresh column data (rather than reusing the earlier session's transform — the live document's whole-model bounding box had shifted by then, ~679 more objects than the earlier check, so the old constants were stale). Cross-validated the new transform two independent ways (matching the site-local column x/z extremes from both ends) — both agreed on the offset to within 0.0001ft, high confidence.
- **The real finding: the anchor's X was already essentially correct; Z was wrong by ~532ft.** Corrected anchor: `x=463.07` (vs. old `462.99` — trivial difference, confirms X was never really the bug), `z=568.37` (vs. old `36.29` — a huge difference, landing near the **opposite end** of the 602.4ft-long site). The old z value was very likely measuring a different object entirely, consistent with the 2026-07-05 note that the *depth* values had needed the same kind of correction for the same reason.
- **Fix applied:** `PershingMetabolizer_Prototype/real_geometry.json`'s `secondary_entrance_anchor.x`/`.z` updated to the corrected values (`top_depth_ft`/`bottom_depth_ft` left untouched — those were separately verified correct 2026-07-05). `generate_phase_scenes.py`'s `METRO_ENTRANCE` reads this same JSON field at runtime, so it picked up the fix automatically, no separate edit needed. `index.html` (the superseded prototype) still has the old value hardcoded — not fixed, low priority.
- **Verified via real rebuilds, before/after:** at `canyon_width=7`, excavation now clusters at real z≈405-594ft (matching the corrected anchor, near the far end of the site) — before the fix, the same setting clustered near z≈0-100ft (the wrong end). 182 voxels excavated at this setting post-fix.
- **Refined further, same session: the anchor now uses the connector's face-center, not its volume centroid — and this incidentally resolved the "invisible at default settings" caveat too.** User added a point in Rhino at the center of the `metroConnection` extrusion's face oriented toward the site (verified: its raw X matches the extrusion's X-min bound exactly, its Y/Z match the extrusion's own centroid exactly — confirmed by direct comparison, not assumed). Converted through the same confirmed transform: `x=378.50, z=568.37` (vs. the volume-centroid value `x=463.07` above) — **only 41.4ft from the site's own column-grid edge, vs. the previous ~126ft.** Re-verified via real rebuilds: at the app's *default* `canyon_width=3`, excavation is no longer zero — **78 voxels**, correctly clustered at the site corner nearest the connector (real x≈297-351ft, z≈486-594ft). The "tuning question" flagged just above is resolved as a side effect of using a more physically-meaningful reference point, not by changing `transit_falloff_ft`/`threshold` at all.
- **Separate, already-known issue — not part of this bug:** `tunnel_endpoints` and `secondary_entrance_mesh` share `x=408.41`, both out of bounds — the already-documented placeholder-tunnel-geometry gap (`HANDOFF_07052026_SLAB_HELIX.md`, "Next up... not started": real twin-tunnel/station-box geometry hasn't been modeled yet), not touched by this fix.

## 2026-07-05 update: Fix A (structural depth cap) shipped, Rhino MCP now live

- **Rhino MCP is now connected and working** (`.mcp.json`'s `rhinoceros3d` server, port 1999 — needed the Rhino-side `mcpstart` command run once per Rhino session). This is now a first-class data source alongside the OBJ/SVG exports — real object queries (layers, bounding boxes) directly from the live `.3dm`, no export round-trip needed for quick checks.
- **Root cause of the "floating columns" problem, confirmed with live data:** `terracing_engine.py` was using `entrance_base_depth_ft` (real data, 63.29ft — the entrance/tunnel's own genuine depth) as the cap on *general* canyon excavation everywhere on site. Real columns are only 30ft tall (`real_geometry["column_height_ft"]`, independently confirmed via live Rhino query of column top/bottom Z). Two unrelated numbers were conflated.
- **Fix shipped:** `TerracingEngine` now has a separate `max_canyon_depth_ft` parameter (defaults to the real `column_height_ft`, not a hardcoded literal), used everywhere depth is scored/capped (`_z_for_voxel`, `_relax_depths`). `entrance_base_depth_ft` is now purely informational (still real data, just no longer misused as a site-wide cap). `vector_export.build_terraced_solid` and `blender_cockpit.build_terrace_mesh` both updated to match (`floor_z` now keyed off `max_canyon_depth_ft`). Verified: engine's excavated-cell max depth now 18-27ft depending on entrance position (previously could theoretically reach 63ft); `run_vector_export_demo.py` and the live Blender cockpit both re-tested clean after the change.
- **`real_geometry.json`'s `secondary_entrance_anchor` corrected** using a live Rhino MCP query against the `metroConnection` layer (old value `bottom_depth_ft=63.29` was actually measuring a *different*, separate object — see below). New anchor: `x=462.99, z=36.29, top_depth_ft=-0.25, bottom_depth_ft=29.75` — cleanly within the 30ft garage envelope now, matches `STRUC__Slabs::L3`.
- **Real per-level floor slabs now exist in the Rhino model**: `STRUC__Slabs::L1/L2/L3`, confirmed via live query at 9.75-10.75 / 19.75-20.75 / 29.75-30.75 ft below grade — matching the previously-diagrammatic 0/-10/-20/-30 assumption almost exactly. This unlocks the Fix B "modular scaffolding where slabs are missing" idea from the earlier handoff — real slab geometry to test against now exists, not just a labeling convention.
- **`metroTunnel` is a separate, genuinely deeper feature, NOT yet correctly modeled** — currently a single rough placeholder extrusion (60ft × 914ft × 74ft, spanning roughly -19.5 to -93.4ft below grade). Per a newer Gemini handoff (2026-07-05), the real intent is: `metroConnection` (vestibule, 0-30ft, now correct) is separate from the actual LA Metro heavy-rail running tunnels (twin 18'10"-diameter cylinders, inverts around Z=-55, running parallel to Hill St under a ~65×30×600ft station box tying into Subfloor 3 at -30ft). **Not built yet** — `tunnel_endpoints` in `real_geometry.json` intentionally left uncorrected for now, to avoid updating it twice once the real twin-cylinder geometry is injected.
- **Ramp verification:** confirmed via live Rhino query that `CIRC__Ramps` (4 objects, 2 clusters × 2 stacked levels) are the *2 circular spiral cores* (outer radius ~54ft, matches a newer handoff's spec), NOT "4 straight portal ramps" as one handoff's phrasing implied — those 4 straight entrance/exit trench ramps (2 Olive St, 2 Hill St) don't exist in the model yet and would need to be modeled from scratch, not adjusted. Ring-splitting the spiral cores (outer=walkway / inner=lightwell) explicitly deferred per user (2026-07-05) — not started.
- **Known open issue, not yet root-caused:** the plan-view SVG export (`pershingRhinoPlanView.svg`) derives wildly wrong overall site dimensions (64×51ft vs. real 354×602ft) via `structural_grid_analyzer.py`'s column-spacing calibration, despite the model itself measuring correctly in Rhino. Not blocking — Rhino MCP now provides a reliable alternative path to the same data — but the SVG export's own scale problem is still unexplained (suspect multiple views/details at different scales on one sheet).
- **OBJ ingestion status:** the freshly re-exported `PershingCurrentMetabolism.obj` (with "layers as OBJ groups" now checked) has not been re-verified since that setting was enabled — `extract_real_geometry.py` still needs updating regardless, since its column-detection heuristic (vertex-count bucket 500-1300) doesn't match this model's much simpler ~6-vertex column proxies, and most named groups were still generic `object_N`/`GroupNN` auto-names as of the last check (before the "layers as OBJ groups" fix). Needs re-inspection before trusting OBJ-based extraction again.

## Next up (not started)

1. Model the twin running-tunnel cylinders + station box via Rhino MCP (18'10" diameter, inverts ~-55ft, parallel to Hill St), replacing the current rough `metroTunnel` placeholder — then correct `tunnel_endpoints` in `real_geometry.json` to match.
2. Model the 4 straight portal/trench ramps (2 Olive, 2 Hill St), 10-12% grade, cutting from sidewalk level — net-new geometry, not an edit.
3. Re-verify the re-exported OBJ (layers-as-groups) and update `extract_real_geometry.py`'s name mapping + column-detection heuristic accordingly.
4. Ring-splitting the spiral ramp cores (outer walkway / inner lightwell) — explicitly deferred, do only when asked.

Context: Gemini drafted a 7-section implementation blueprint (`TODO_ThesisFinal_Pipeline_Blueprint.md`, archived at `archive/memoryMachine/`) for rebuilding Memory Machine as a universal, portable site-intervention pipeline (not hardcoded to Pershing Square). This document summarizes what's been built and verified against that blueprint, and what's left.

## What's been decided (architecture, locked)

- **No hard deadline.** ~2 months total runway; an earlier self-imposed "Monday" milestone was not a real instructor deadline — just move fast.
- **Local-only Unity for now.** Networked multi-device juror interaction (phones/tablets joining live) is a stretch goal for later, not blocking the core build. Input-event schema is kept transport-agnostic so it can be added later without a rewrite.
- **Ollama** is the confirmed local-LLM platform for Section 7 (live juror chat), chosen to avoid API costs during a live presentation. Hardware (either the user's home or school PC, both with 64GB RAM and a modern GPU) comfortably fits 7B–14B models. Not started yet.
- **Grid is derived, not hardcoded.** The 27ft Pershing column grid is read from real Rhino-exported geometry (SVG plan/elevation + OBJ), using one known reference dimension as a calibration constant — not baked in, so the pipeline can point at a different site.
- **Columns stay as existing structure.** The old "4–5ft keep-out-zone" constraint (don't excavate near columns) was explicitly cut. The parking garage's column grid is now the *organizing framework* for the design, not an obstacle.
- **Clean diagrams always export.** The DXF/SVG/PNG "clean line" pipeline (Section 6) runs on every render, and will keep doing so once the Blender high-fidelity/eroded pass matures — that pass adds its own output folder, it doesn't replace this one.
- **Pre-Blender geometry is deliberately "clean"** (no erosion) for Sections 1–3/6, so bugs in slicing/export logic can't be confused with bugs in a weathering pass. Erosion is a separate, later stage (Section 4), now rehearsed.

## What's been built and verified (this session)

### Phase 1 — Universal Site Ingestion (Section 1)
- **`structural_grid_analyzer.py`** — extracted and generalized from the old `urban_interference_solver.py`. Reads column/slab geometry from a Rhino SVG plan-view export, derives real column spacing via clustering + median (not hardcoded), also derives garage depth from elevation SVGs. Layer names and cluster tolerance are now parameters, not fixed strings — portable to a different site's export.
- **`extract_real_geometry.py`** (OBJ ingestion) — also generalized: the OBJ object-name mapping (which named group is the tunnel, columns, etc.) is now a parameter, not hardcoded, verified byte-identical output for Pershing before/after the refactor.
- Both ingestion paths (SVG-derived and OBJ-derived) independently agree on site dimensions (~354 × 602 ft) and column count (274) — cross-validated, not just asserted.

### Phase 2 — Terracing Engine ("Metabolizer v2") (Section 3)
- **`terracing_engine.py`** — headless Python port of the canyon-terracing math that used to live only in client-side JS (`PershingMetabolizer_Prototype/index.html`). Verified **byte-for-byte identical** to a standalone Node.js re-implementation of the original JS, across all 2,680 voxels on real Pershing data.
- Column keep-out-zone logic confirmed absent (per the locked decision above) — only the spiral parking ramps are a hard excavation-avoidance zone, since those are real physical voids, not columns.
- Added the Botanical Attractor Module's depth-level tagging (Level 0 / -1 / -2 / -3), which didn't exist anywhere before.
- **Bug found and fixed (2026-07-03):** voxels were originally modeled as solid "shafts" running from grade down to each voxel's own depth — meaning every voxel's *top* face sat at grade, and the only surface near a voxel's real depth faced *downward* (invisible from above). Rebuilt so every voxel runs from its own top (grade, or its excavation depth) down to a shared floor reference — now every terrace step has a real, upward-facing tread surface, which is what per-level surface conditions (shade canopy / planters / structural sub-vaults) actually need to attach to.

### Phase 3 — Vector Export Engine (Section 6)
- **`vector_export.py`** — builds an actual 3D solid from the terracing engine's output plus the real static context (columns, tunnel, secondary entrance, ramps), then does **true mesh-plane intersection** for section/plan cuts (not a voxel-grid approximation), and a real **ray-cast hidden-line-removal** pass for the axonometric view (silhouette + crease + boundary edge detection, embreex-accelerated — full HLR on a 552k-face mesh in ~1.5s).
- Exports to **DXF + SVG + PNG** together (PNG via matplotlib, sharing the same layer/color scheme as the SVG writer — added per user request for a quick visual reference alongside the CAD deliverables).
- Layer scheme: `LAYER_01_CUT`, `LAYER_02_PROJECTION` (currently empty — needs real hidden-line visibility logic for background-beyond-cut geometry), `LAYER_03_BOTANICAL` (empty — no planting geometry generated yet), `LAYER_04_GRID` (column reference points), `LAYER_05_LABELS` (street names).
- Elevation tags (`EL. -10'-0"` etc.) on every plan level; named levels: SURFACE / LEVEL 1 / LEVEL 2 / LEVEL 3-METRO. Total garage depth (30ft) is real/measured; the 3-equal-levels split is still a diagrammatic assumption inherited from the old prototype, not surveyed.
- **Orientation bug found and fixed:** an unverified assumption (which street is which end of the site) had been carried over from a different, separately-derived dataset without cross-checking. Corrected against the real Rhino model, verified via exact screen-projection math, not eyeballing. Confirmed the site's own grid runs ~36° off true north by design (not a bug) — so plan/axo drawings export in **both** page-layout orientations (`_5thup` / `_6thup`) since "which end is up" is a layout choice, not a geometric fact.
- Axonometric camera direction tuned per user feedback (verified numerically each time, not by repeated screenshotting) to put Hill St / the Metro entrance in the desired frame position.

### Blender bridge — Line Art rehearsal (Section 4/6 crossover)
- **`blender_lineart_export.py`** — imports the clean site OBJ (`up_axis='Z', forward_axis='Y'` required, or Blender silently rotates it wrong), runs Blender's own Grease Pencil Line Art (a second, independent hidden-line-removal algorithm from the Python one), exports to SVG.
- Real bug found (not a GPU/headless limitation, despite first appearances): manually building a Grease Pencil object + Line Art modifier reliably bakes to **zero strokes** in any environment. Fix: use Blender's dedicated `LINEART_OBJECT`/`SCENE`/`COLLECTION` presets instead, and size the camera's `ortho_scale` from the mesh's bounding box projected into **camera space**, not world-space dimensions.
- Verified working both headless and in a live BlenderMCP-connected session; cross-validated visually against the Python HLR axo (same terrace/entrance/tunnel silhouette from two independent algorithms).

### Blender bridge — Erosion/weathering rehearsal (Section 4)
- **`blender_erosion_pass.py`** / **`blender_erosion_render.py`** — exports the site as a *named* multi-part OBJ (terrace / columns / tunnel / entrance / ramps as separate Blender objects, via `vector_export.py`'s `build_named_scene()`), so erosion can target the terrace only, leaving real existing structure clean/unweathered.
- Subdivides the terrace, applies a Displace modifier driven by turbulent noise, plus a weathered-concrete shader material (noise → ColorRamp → Base Color, patchy dark staining mixed with a lighter weathered gray).
- Iterated from a too-subtle first pass to a convincing "30-year weathered" look per direct feedback (stronger displacement + real material variation, not just geometry). Verified via real EEVEE renders, including a 4K (3840×2160) final check.

### Sketch weight ingestion (Phase 2 extension, per a Gemini handoff)
- **`sketch_weight_mapper.py`** — lets the designer draw an intentional layout (SVG or raster image) that biases the terracing engine, instead of (or alongside) the automated proximity/data-driven excavation. SVG parsed via `svgpathtools` (real bezier curves — the existing regex SVG parser only handles straight Rhino CAD polylines, wrong tool for a freehand sketch); image ingestion via PIL, prefers the alpha channel over luminance thresholding when present.
- Scoped to **plan sketches only** this round — a section sketch would mean directly overriding z-depth along a cutline (the engine is a 2.5D heightfield, one z per (x,y) column, not a true 3D volume), a different mechanism than a blendable weight. Deferred as a separate future feature.
- Blend is **additive** (`terracing_engine.py`'s `_effective_influence()`: `transit_influence + alpha*sketch_weight`, clamped), chosen deliberately over two other options (soft multiplicative gate / hard mask) so the sketch can *create* real excavation somewhere the Metro-proximity data alone never would — not just edit or restrict the data's own proposal. `alpha` defaults sketch-dominant (0.75).
- **Caught and fixed a real bug via testing:** the first version used a weighted *average* (`alpha*sketch + (1-alpha)*transit`) instead of addition, which silently dampened data-driven excavation almost to zero everywhere the sketch was blank — i.e. everywhere except the sketch itself — wiping out all 424 originally-excavated cells. Fixed to literal addition; re-verified all 424 original cells unchanged (zero depth difference) after adding a test sketch, plus 119 new cells added elsewhere.
- Visual proof: a plan render shows two independent excavated regions — the original entrance-driven gap, and a new S-curve-shaped gap matching a synthetic test sketch exactly, in a corner of the site the data alone would never touch. Archived at `archive/diagrams/PershingMetabolizer/sketch_ingestion_test_20260703/`.
- Coordinate convention (sketch assumed to cover the full site bounds, no vertical flip) is explicitly flagged **unverified** in the module — no real designer sketch exists yet to confirm orientation against, and this project has hit real axis-flip bugs more than once already.

### Design-agency planning session (2026-07-04, no code yet)
- User laid out a full **Surface Typologies** taxonomy (Green Space; Amenities incl. Necessities; Hardscape incl. Flexible Space + Circulation; Water Features; Main Attractor = Canyon + Lightwell honoring the existing Pershing Square fountain). Full detail, plus the resulting decisions below, written up at `archive/memoryMachine/Design Plan - Surface Typologies & Agency Sliders.md`.
- **Locked authority hierarchy:** the real site SVG/grid (from `structural_grid_analyzer.py`, i.e. the Rhino due-diligence data) is ground truth and is never distorted to fit the sketch; the sketch is fit *within* that real frame and, within it, remains the primary design author (`sketch_alpha` stays high).
- **Sketch color-coding will be staged, not built all at once:** black=canyon (done) → blue=hardscape/protect (next, needs new flood-fill-interior logic since it's a region claim, not a line) → green/others later, likely combined with written labels for sub-types (the user already hand-labels sketches, e.g. street names) rather than trying to find 7+ distinguishable colors.
- **Slider scope discussed:** `sketch_alpha` (agency dial, already real), x/y/z position offset, canyon "width" (maps to `sketch_weight_mapper.py`'s `falloff_scale_ft`), canyon depth multiplier. Canyon **shape** stays explicitly NOT slider-controlled — sketch+boundary-derived only, per the user directly.
- **Architecture decision: Blender becomes the live control "cockpit"**, tried before any new app or Three.js work. Blender's own Python can directly `import` `terracing_engine.py` / `sketch_weight_mapper.py` / `vector_export.py` (proven feasible earlier when `smolagents` was pip-installed straight into Blender's bundled Python) — no server, no duplicated logic. Sliders are real Blender properties; changing one re-runs the real engine and rebuilds the terrace mesh in-viewport. Final DXF/SVG/PNG export can also be triggered from that same Blender session. The Three.js Metabolizer app is explicitly NOT touched — stays the separate fast-diagramming tool.
- **Deferred:** an in-app digital circle/outline/paint markup tool (instead of hand-drawing + photographing) — good idea, intentionally not started yet.
- **Critical-review conclusion, still pending build:** don't build the full slider panel yet — most of the discussed sliders (moving discrete typology objects) assume a data model (Stage 2+ color regions) that doesn't exist. The one genuinely unverified bet is whether Blender can live-recompute + rebuild the terrace mesh at a workable speed/feel at all. Plan is to prove that first with a single working `sketch_alpha` slider before adding anything else.

### Blender cockpit proof-of-concept — built and PROVEN workable (2026-07-05)
- **`blender_cockpit.py`** — imports `terracing_engine.py` directly into Blender's own Python (no trimesh/vector_export dependency in Blender at all — a plain bmesh box-per-voxel port of `vector_export.build_terraced_solid`, since trimesh/embreex are too heavy to assume are installable into Blender's bundled interpreter). Sketch weights are precomputed outside Blender by the new **`precompute_sketch_cache.py`** (runs in the normal venv, which has PIL/svgpathtools) and cached to plain JSON, so Blender's Python never needs those either.
- Exposes one Scene property, `mm_sketch_alpha`, in a "Cockpit" N-panel tab; the update callback reruns `TerracingEngine` and rebuilds the terrace mesh in place.
- **The one genuinely unverified bet from the 2026-07-04 planning session is now settled: yes, this is workable.** Verified live via BlenderMCP — full rebuild (2,680 voxels, 16,080 faces) took **0.04–0.05 seconds**, both directions (0.75 → 0.0 → 0.75), each confirmed visually via viewport screenshot showing the expected excavation change (most of the site flattens to grade at alpha=0 except the entrance-driven corner, as expected). Fast enough that a real-time slider drag should feel fully live, not laggy.
- Static context (columns/tunnel/secondary_entrance/ramps) is imported once from the existing `outputs/vector_export_test/site_named.obj` for visual reference, not rebuilt.
- **Orientation double-checked and fixed (same session):** the first round of cockpit screenshots appeared to show the Metro entrance on the wrong side. Verified numerically (not by eyeballing) that `real_geometry.json`'s coordinates were never wrong — projecting the entrance through `vector_export.py`'s already-validated `AXO_VIEW_DIR`/mirror convention puts it at the largest +u (rightmost) of any reference point, matching that module's own prior verification. The actual cause: `blender_cockpit.py` was using Blender's arbitrary auto-frame camera (`view_selected`), not this project's established axo convention. Added `setup_axo_view()` to the script, reproducing the same view_dir/mirror/basis in Blender (object `matrix_world` mirror, not mesh-data mutation, so it stays correct across live terrace rebuilds) — confirmed visually via a fresh screenshot that the entrance/tunnel now read on the right, consistent and reproducible on a clean re-run.
- **Next slider work should build on this proven base**, not re-litigate whether live rebuild works or re-doubt site orientation — both are now numerically and visually confirmed.

### Sketch ingestion — flip bug fixed, Stage-2 hardscape regions added (2026-07-05)
- Fixed a real bug in `build_sketch_weights()`: the SVG path never forwarded `flip_x`/`flip_y` overrides (only the raster/image path did) — now both paths honor an explicit override, defaults unchanged.
- Added `find_color_regions()` (raster, hand-rolled flood-fill/connected-components — no scipy/shapely added, matched existing project dependency footprint) and `find_closed_svg_regions()` (SVG, closed-subpath detection) plus a `build_hardscape_regions()` dispatcher. Returns `[{"type": "hardscape", "mask": (nx,nz) bool array}, ...]` — same grid shape convention as `sketch_weights`, so it can plug into the engine the same way once that wiring is built.
- **Known, documented limitation:** a blue outline with even a 1px gap won't fill (leaks to background, silently yields no region) — not fixed, flagged in the docstring.
- **Still unverified against a real sketch** — no blue-marked sketch exists yet in `data/sketches/` (the one real sketch on hand is black-line-only); only tested against synthetic PIL/SVG test fixtures, same bar the original black-line ingestion cleared before a real sketch existed.
- Not yet wired into `terracing_engine.py`'s blending logic — that's the next step, not done here.

### Infra fix: BlenderMCP couldn't connect (2026-07-05)
- Root cause: `getaddrinfo("localhost")` fails on this specific machine (a local DNS/hosts misconfiguration), so `uvx blender-mcp`'s default hostname-based connection silently failed even though Blender's addon server was correctly running the whole time — everything *looked* connected (Blender addon panel, Claude Code's own MCP status) except the actual command round-trip, which is what made this hard to spot.
- Fixed by pinning `BLENDER_HOST=127.0.0.1` as an env var in `.mcp.json`'s `blender` server entry. This is a machine-specific fix; since the repo syncs across multiple PCs via Syncthing, if `localhost` resolves fine on another machine this change is still harmless there (127.0.0.1 always works), but if BlenderMCP mysteriously fails to connect on a *different* machine despite everything looking "connected," this DNS quirk is probably not the cause there — check that machine's own `getaddrinfo` behavior before assuming this fix generalizes.

## What's still open / not started

*(Reconciled 2026-07-08 against what's actually shipped since 07-05 — several items below the 07-05 snapshot listed as open are now done; see strikethrough-style notes.)*

- ~~Wire hardscape regions into the cockpit/engine~~ — **done.** Both `blender_cockpit.py`'s `rebuild_all()` and `logic/pershing_api.py`'s `rebuild()` pass `hardscape_regions` into `TerracingEngine` as a hard veto (`is_hardscape` → excavation forced to 0 regardless of score). Applied unconditionally now (the separate "protect hardscape" toggle was removed 2026-07-06 as redundant — painting a region already means protect it).
- ~~Sketch ingestion has no real test case~~ — **superseded, not resolved in its original form.** The file-based SVG/image sketch-ingestion workflow (`sketch_weight_mapper.py`'s SVG/raster parsing) was superseded by **live painting** in both the Blender cockpit (Image Paint canvas over an uploaded sketch photo) and the web app (`PaintOverlay.jsx`, HTML canvas over the same photo) — real designer paint strokes on a real uploaded site photo now drive `SKETCH_WEIGHTS`/`HARDSCAPE_MASK`/`WATER_SHADE_MASK`/`GREENSCAPE_MASK`/`AMENITY_RESTING_MASK` directly, no file-based sketch needed. The original SVG/image-file ingestion code still exists but is no longer the primary path.
- ~~Entrance-anchor / transit-attractor bug~~ — **fixed 2026-07-08, including the default-settings caveat** (see section above). Refined to the connector's face-center point; the attractor now produces real excavation (78 voxels) at the app's default `canyon_width=3`, no tuning changes needed.
- **Section 2 (data ingestion):** amenity-deficit data is still a placeholder/diagrammatic stub (2 hardcoded hotspot points) — no real scraped/surveyed dataset feeding the terracing engine yet, though `blender_cockpit.py`/`pershing_api.py` both now have a `mm_use_real_amenity_data`/CSV-loading path ready for one.
- **Section 4 remainder:** erosion currently applies uniformly across the whole terrace, and only exists as a manual/rehearsal script (`blender_erosion_pass.py`), not wired into the new headless-Blender build tier. Next natural steps, in order: (a) route erosion + the weathered-concrete shader through `blender/pershing_headless_build.py` so "Build in Blender" can optionally produce a weathered/eroded export, not just a clean one; (b) vary intensity/character by the Botanical Attractor Module's per-level tag; (c) real organic vegetative growth (not just concrete weathering); (d) mesh decimation for Unity real-time performance.
- **Section 4/6, Line Art:** ~~not wired into the new headless-Blender build tier yet~~ — **fully done 2026-07-08, end-to-end.** `blender/pershing_headless_build.py`'s `--lineart-output` mode (which also fixed a confirmed-upstream Blender SVG-export bug along the way), `logic/pershing_blender.py`/`app.py`'s wiring (`svg_url` job field, `lineart` query param), and a new frontend checkbox + `LineArtOverlay.jsx` display/download component are all built and browser-verified against the real running app (see dedicated subsections above).
- **Section 5 (Unity sandbox):** not started at all. Local-only decided; networked juror devices deferred.
- **Section 6 remainder:** the "AI-Ready Lineart Rasterizer" (high-contrast raster conditioning images for future ControlNet/LoRA use) hasn't been built. `LAYER_02_PROJECTION` (background-beyond-cut geometry) and `LAYER_03_BOTANICAL` (planting geometry) are real, intentional gaps in the vector export — both need actual content, not just an empty layer.
- **Section 7 (live juror LLM governance):** not started. Ollama + hardware confirmed, but no chat interface, no JSON-schema-constrained parsing, no architectural weight matrix / agency dials, no saturation logic yet.
- ~~Minor known bug: the standalone headless Line Art script occasionally emits one numerically-degenerate coordinate...~~ — **this was a significant underestimate of a real bug, root-caused and fixed 2026-07-08.** See the dedicated "Line Art SVG export" subsection above — it wasn't one cosmetic outlier coordinate, it was a confirmed upstream Blender exporter bug (#136312 / T86942) corrupting the whole SVG's X/Y scale, fixed by bypassing the buggy operator with a custom stroke-point exporter.
- **Garage level split (10ft × 3):** still a diagrammatic assumption, not surveyed real per-level slab data — flagged, not fixed.
- **Section-sketch depth override** (direct z-profile drawing along a cutline) — deferred, a different mechanism than the plan-sketch weight map.
- **Grease Pencil sketch-drawing alternative** — an earlier, working, in-viewport Grease Pencil-based drawing interface (as opposed to the current Image-Paint-based one) exists only as compiled bytecode in a long-running Blender session's memory as of 2026-07-08 (never committed to git). Design spec is documented (`HANDOFF_07052026_GREASE_PENCIL_INTERFACE.md`) and a bytecode/docstring snapshot was captured (`archive/blender_cockpit_grease_pencil_bytecode_snapshot_20260708.txt`) as insurance, but exact source was not recoverable. Not currently planned to be rebuilt (Image Paint superseded it), flagged here only so the snapshot isn't forgotten if ever needed.

## Suggested next step

**Sequence, in priority order (2026-07-08):**
1. ~~Route the erosion/weathering pass and Line Art export through the new headless-Blender build tier~~ — **Line Art done, end-to-end, browser-verified.** `pershing_headless_build.py`'s `--lineart-output` mode, `logic/pershing_blender.py`/`app.py`'s wiring, and a new frontend checkbox + `LineArtOverlay.jsx` display are all built and verified against the real running app (see subsections above). Erosion/weathering is explicitly paused per the user (2026-07-08) — do not resume without it being brought back up.
2. ~~Fix the `pershingRhinoPlanView.svg` site-dimension corruption~~ — **fixed 2026-07-08.** The `STRUC__Slabs` combined-bounding-box approach broke three different ways across three real re-exports of the same site in one session (798.88×1164.07, then 650.61×2711.97, each a *different* wrong shape, not the same bug recurring) — pointing to sensitivity to whatever incidental geometry a given export happens to sweep into that group, not one fixable parsing assumption. Replaced site-extent derivation in `structural_grid_analyzer.load_structural_grid_from_svg()`: now prefers a purpose-drawn `BOUNDARY` curve when present, falling back to the old `STRUC__Slabs` bbox only when no boundary layer exists, plus a `[WARN]` print when both are present and disagree by >5ft (fires correctly against the still-broken slab bbox — confirmed live, not just in theory). Verified against a real re-export: `356.34 × 606.14 ft` vs. real `354.14 × 602.4` (0.6% error) — vs. the old approach's `650.61 × 2711.97` on the *same file*.
   - **Side effect from this session's debugging, now fully recovered:** while scripting a test `-Export` via Rhino MCP's `run_command`, a chained `_Enter` token after a failed (nothing-selected) export attempt very likely re-triggered Rhino's Export dialog against its last-remembered filename, overwriting `pershingRhinoPlanView.svg` with an empty SVG shell. The live Rhino model itself was never affected (confirmed via `get_document_summary` at the time). User re-exported the plan view; re-verified the fix directly against the real file: `353.04 × 600.53 ft` vs. real `354.14 × 602.4` (~0.3% error) — and the `STRUC__Slabs` cross-check fired again, giving a *fourth* differently-wrong value (`968.39 × 1560.20`) on this fresh export, further confirming `BOUNDARY` was the right call rather than a one-off fix.
   - **The transit-entrance anchor is now SVG-derivable too — the live-Rhino-MCP-query workaround from earlier this session is retired.** A plain Rhino POINT object (the face-center marker used for the earlier fix) was confirmed NOT to survive this SVG export at all — no fill/stroke for the exporter to emit. Switched to a small circle marker instead (same convention as columns), placed under a new `metroEntrance` layer. `load_structural_grid_from_svg()` now reads it (new `entrance_layer` param, default `"metroEntrance"`) and returns `entrance_anchor_ft` (`None` when the layer/marker is absent — verified gracefully non-crashing against a file without it). Verified against the real re-exported file: `(378.51, 566.27)` vs. the already-applied live-query value `(378.50, 568.37)` — matches to 0.01ft on X, 2.1ft on Z, from two fully independent measurement paths. Site dimensions from this same export are now `354.22 × 602.53 ft` against real `354.14 × 602.4` (~0.1ft error) — the tightest result yet.
   - **Unified builder written: `PershingMetabolizer_Prototype/build_real_geometry.py`.** Merges SVG-derived (`structural_grid_analyzer.py`) site extent/column positions/entrance anchor with OBJ-derived (`extract_real_geometry.py`) volumetric geometry (tunnel/entrance/ramp meshes) into one `real_geometry.json`, per the SVG-as-source-of-truth authority split scoped this session. Cross-validates and warns (doesn't block) whenever sources overlap. (Grew into a genuine three-way merge later the same session — see below.)
     - **Hit and fixed two more pre-existing, previously-undiscovered bugs while wiring this up.** (1) `extract_real_geometry()`'s column-detection heuristic finds **zero** columns in the current grid-base OBJ (already flagged as fragile 2026-07-05, now confirmed fully broken, not just imprecise) — crashed on `column_positions[0]` since that's also how `grade_y` (the depth-below-grade reference for every other mesh) gets established. (2) Its `secondary_entrance_name="object_2"` mapping is stale and gives `bottom_depth_ft=63.29` — not a new bug, this is *the exact same* wrong value the very first 07-05 status entry already documented as measuring a different object, just rediscovered fresh rather than reintroduced. Fixed pragmatically rather than by chasing either heuristic: added `grade_y_override` to `extract_real_geometry()` (a physical site constant, doesn't need re-deriving every run) and made the builder bootstrap both `grade_y` and the entrance anchor's depth values from whatever's already at `real_geometry.json`, only trusting fresh OBJ extraction when no existing value is available. Verified the fix by deliberately re-running against a restored known-good baseline and confirming the `[WARN]` prints fire and correctly resolve in favor of the existing values, not the stale OBJ ones.
     - **Verified end-to-end after merge:** backend restarted, `/api/pershing/config` reflects the merged site dimensions (354.22×602.53), a real `/api/pershing/rebuild` at default settings gives 79 excavated voxels (vs. 78 with the earlier hand-applied fix — consistent given the ~2ft independent-measurement variance already noted above).
     - ~~Both remaining heuristic bugs stay open~~ — **actually fixed at the root shortly after, same session, superseding the bootstrap above.** User pointed out the app's static geometry didn't reflect the most current OBJ export (`PershingCurrentMetabolism.obj`, exported today with "layers as OBJ groups" enabled — real Rhino layer names as `g` groups, e.g. `g STRUC__Columns` containing 295 real per-instance `o` objects, `g BOUNDARY`/`g metroConnection` each with exactly 1 — all counts independently matching live Rhino MCP queries taken earlier this session). Extended `parse_obj()` to track `g`/`o` group membership (additive, no external callers, safe) and added `extract_positions_from_layered_obj()` — reads position/depth facts (not mesh geometry, most groups in this export have vertices but zero faces) by real layer name instead of the fragile vertex-count heuristic or stale generic `object_N` mapping. Verified directly: site extent `354.14×602.4 ft` (**exact** match to real, tighter than SVG's own 354.22×602.53), grade_y matches the live Rhino value to full float precision, entrance-anchor depth matches the already-verified `-0.25/29.75` exactly. One real nuance caught, not assumed away: this file's `metroConnection` object's own x/z reproduces the *exact original pre-fix bug value* (`462.99, 36.29`) — a stale snapshot predating the live-Rhino correction verified earlier this session, not a new bug — so the builder deliberately keeps SVG authoritative for entrance x/z specifically while trusting this new source for site extent, column positions, `grade_y_raw`, and `ramp_anchors`. `build_real_geometry.py` is now a genuine three-way merge (SVG + layered OBJ + grid-base OBJ for mesh-only fields); no more file-bootstrapping needed for `grade_y`/depth. Re-verified end-to-end: backend restarted, real rebuild at default settings gives 79 excavated voxels, 742 structural elements, 319.95 tons slab harvest.
   - Also found while investigating (2026-07-08): a Detail viewport scale entered as "1:96" in this Feet-unit document actually computes to 1:1152 (=96×12) — the classic feet-vs-inches trap (1"=96' instead of the intended 1"=96"=8'). Cosmetic for the printed sheet, doesn't affect the calibration above (which self-derives scale from real column spacing regardless of print scale) — worth re-entering as `1:8` on the Rhino side if the printed drawing's own scale label matters.
3. **Get a real blue-marked (and other-category) sketch** from actual live painting sessions to replace/supplement the synthetic test fixtures the hardscape/water-shade/greenscape/amenity-resting region code has mostly only been verified against so far.
4. Section 2's real amenity-deficit data ingestion (a Google Forms export dropped into a folder, per user's stated intent, replacing the 2-hotspot placeholder) — the CSV-loading path already exists (`amenity_deficit.py`), just needs real data.

Sections 5/7 (Unity, live LLM chat) remain lower priority, unchanged from the 2026-07-05 assessment: user intends a **full Unity build** (not a pre-baked walkthrough) despite being newer to Unity — recommend a minimal derisking spike (import one exported mesh, walk it with the default controller) before investing in polish there. Section 7's live-LLM-governance idea may not need a bespoke Ollama chat UI at all — if the cockpit's sliders are implemented as clean, named Blender operators/properties (as `blender_cockpit.py` already does for `sketch_alpha`) or clean FastAPI routes (as the new hybrid pipeline does), an LLM agent can drive the same operators/routes via tool-use, meaning Section 5 and Section 7 can share one implementation instead of being two separate builds.
