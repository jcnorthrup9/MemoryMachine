# Memory Machine — Claude Handoff & Architecture Summary
**Date:** 2026-04-07
**State:** Backend Modularized, Frontend UI Degraded

---

## What Just Happened (The Modularization Sweep)
We recently completed a strict architectural refactor to de-bloat the main application files. 
1. **Python Logic Extracted:** `app.py` is now strictly a FastAPI routing switchboard. All heavy lifting was moved to `logic/ai_synthesizer.py`, `logic/urban_engine.py`, and `logic/geometry_engine.py`.
2. **CSS Centralized:** We extracted hundreds of lines of inline CSS from `zine_compiler.py`, `deck_compiler.py`, and `scrapbook_compiler.py` into a unified `static/master_style.css`.
3. **JS Modularized:** We split the monolithic frontend JS into `static/js/constants.js`, `static/js/state.js`, `static/js/engine2d.js`, and `static/main.js`.

---

## The Current Problem
During the JS and HTML migrations, the main application UI (`index.html`) lost its intended styling and functionality. 
- There are lingering Git merge conflicts (`<<<<<<< HEAD`) inside `main.js` and `templates/index.html`.
- The UI tabs (2D Canvas / 3D Preview) and buttons (Lofi toggle, Draft/Dream toggle) are visually broken or unresponsive.
- The main app styling does not reflect the unified "Retro-Futuristic Cyberpunk / Analog Archival" aesthetic defined in `master_style.css`.

---

## Claude's TODO List

### Priority 1: Purge Merge Conflicts & Sync JS
- Audit `main.js`, `templates/index.html`, and `index.html`. 
- Remove all `<<<<<<< HEAD` merge conflicts. 
- Ensure `main.js` correctly imports and utilizes the state from `static/js/state.js` and `static/js/constants.js`.

### Priority 2: Restore the UI & Aesthetic
- Fix the DOM structure in `index.html` so the layout matches the intended split-panel design (Terminal on the right, Viewports on the left).
- Ensure the CSS for the main app (either in `static/style.css` or by borrowing from `master_style.css`) brings back the retro-terminal, glowing accent (`#fff4ca`), and dark-mode aesthetic. 
- Re-wire the tab navigation so switching between the 2D SVG canvas and the 3D Three.js viewport works flawlessly.

### Priority 3: Verify the 3D Viewport
- The backend `app.py` successfully returns an array of `geometries` (boxes, cylinders, spheres). 
- Ensure `main.js` correctly parses this array and renders it using Three.js when the "Generate Intervention" button is clicked.
- Adjust Three.js lighting and materials to look sleek and architectural.

### Priority 4: ComfyUI / Blender "Dream" Hookup
- Once the Draft UI is fully restored, prepare the frontend to handle the "Dream ✦" mode toggle.
- Ensure the frontend can accept a `.glb` URL from the `/api/generate` endpoint and load it into the Three.js scene via `GLTFLoader`.

---

## Rule of Engagement
- **DO NOT** revert or bloat `app.py`. It must remain a clean FastAPI router.
- **DO NOT** put inline CSS back into the Python compiler files in the `logic/` folder.
- **DO** aggressively fix, style, and refactor the HTML/JS/CSS on the frontend.